#!/bin/bash

#This script will start a docker apache web server
#https://hub.docker.com/_httpd

# this is teh default from Apache
docker run -dit --name apache01 -p 8080:80 -v "$PWD" :/usr/local/apache2/htdocs/ httpd:2.4
